# تدفق اللعبة والأجهزة — «الملف المفقود»

هذه الوثيقة تشرح **كيف تتحرك اللعبة بين الأجهزة**: من يملك القرار، ماذا يُكتب أين،
وما الذي يستطيع كل جهاز قراءته. القواعد نفسها في [`GAME_RULES.md`](GAME_RULES.md).

---

## 1. الأدوار التقنية للأجهزة

### الجهاز الرئيسي (Host)

جهاز واحد فقط، وهو **مقدّم اللعبة**:

- المصدر **الوحيد** للصوت: التعليق، الموسيقى، المؤثرات، العد التنازلي.
- المالك **الوحيد** لحق الانتقال بين المراحل (`room/phase`).
- يعرض المعلومات العامة فقط: الأسماء، ترتيب الجلوس، المرحلة، المؤقت، الحالة.
- **لا يعرض أبدًا**: دور لاعب آخر، نتيجة نرده، موعد استيقاظه، أو صوتًا واحدًا
  يوحي بأي من ذلك — قبل شاشة النتائج.

### جهاز اللاعب (Player)

- يعرض معلومات صاحبه السرية فقط.
- **صامت تمامًا**: لا صوت، لا موسيقى. مسموح فقط بالاهتزاز والتنبيهات الصامتة.
- لا يستطيع تغيير مرحلة الغرفة؛ يتبع `room/phase` دائمًا.
- يكتب فقط في مساره الخاص: جاهزيته، تأكيداته، نرده، فحصه، صوته.

### Firebase

- مصدر الحقيقة للحالة المشتركة، ووسيط المزامنة اللحظية.
- **لا يحتوي** على منطق يحسم اللعبة في العميل: توزيع الأدوار والنرد والتحويل إلى متعاون
  كلها عمليات تُكتب مرة واحدة في مسارات سرية لا يقرأها إلا صاحبها.

---

## 2. آلة الحالة (State Machine)

المضيف هو الوحيد الذي يكتب `room/phase`. كل جهاز يستمع ويعرض الشاشة المقابلة.

```
home ──► create-room ──► lobby ◄──► seating-order
                          │  ▲          │
                    join-room │      settings
                          └──┴──────────┘
                              │  (المضيف: ابدأ توزيع الأدوار)
                              ▼
                      role-distribution
                              │  (كل اللاعبين: فهمت دوري)
                              ▼
                          dice-mode ──► dice-roll
                                            │  (كل اللاعبين: عرفت موعدي)
                                            ▼
                                       ready-check
                                            │  (المضيف: ابدأ الليل)
                                            ▼
                                       night-intro
                                            ▼
   night-phase-1 ► night-phase-2 ► night-phase-3 ► night-phase-4 ► night-phase-5 ► night-phase-6
                                            ▼
                                    night-accomplices
                          (المُخفي وحده يفتح عينيه ويختار من يساعده)
                                            ▼
                                     secret-actions
                                            │  (كل اللاعبين أنهوا إجراءاتهم السرية)
                                            ▼
                                       discussion
                                            │  (المضيف: انتقل للتصويت)
                                            ▼
                                         voting
                                            │  (كل الأصوات وصلت)
                                            ▼
                                         reveal ──► results
                                                       │
                                    ┌──────────────────┴──────────────────┐
                                    ▼                                     ▼
                            lobby (جولة جديدة)                        home (إنهاء)
```

انتقالات استثنائية متاحة في أي وقت للمضيف: `paused` (تجميد الجولة) و `aborted` (إلغاء).

### شروط الانتقال

| من | إلى | من يطلق الانتقال | الشرط |
|---|---|---|---|
| `lobby` | `role-distribution` | المضيف | 4 ≤ لاعبون ≤ 8، الجميع `ready`، الجميع متصلون، ترتيب الجلوس مؤكَّد |
| `role-distribution` | `dice-roll` | تلقائي | كل اللاعبين أرسلوا `roleAck` |
| `dice-roll` | `ready-check` | تلقائي | كل اللاعبين أرسلوا `diceAck` (وفي وضع الأربعة: `chosenSlot` لأعضاء الفريق) |
| `ready-check` | `night-intro` | المضيف | تأكيد يدوي بعد قائمة تحقق |
| `night-intro` | `night-phase-1` | المضيف (مؤقت) | انتهاء نص «الجميع يغلق عينيه» |
| `night-phase-N` | `night-phase-N+1` | المضيف (مؤقت) | انتهاء العد التنازلي ونص الإغلاق |
| `night-phase-6` | `night-accomplices` | المضيف (مؤقت) | انتهاء العد التنازلي ونص الإغلاق |
| `night-accomplices` | `secret-actions` | المضيف (مؤقت) | وصول اختيار المُخفي، أو انتهاء العد ثم اختيار احتياطي، ثم نص «انتهى الليل» |
| `secret-actions` | `discussion` | تلقائي | كل جهاز أرسل `secretAck` |
| `discussion` | `voting` | المضيف | يدوي |
| `voting` | `reveal` | تلقائي | كل اللاعبين المتصلين صوّتوا |
| `reveal` | `results` | المضيف (مؤقت) | انتهاء الكشف السينمائي |

المؤقتات تعمل على جهاز المضيف فقط ثم يكتب المرحلة الجديدة — لا يوجد مؤقت موزّع.

### الليل بلا تعليق صوتي

حين يُطفأ الراوي (`voiceEnabled: false`) لا شيء يقود المراحل: لا جملة تُقال ولا
عدّ يمشي. فيقودها المضيف بنفسه — **والشاشة كلّها هي الزر**، لأن عينيه مغمضتان
مع الطاولة فلا يبحث عن زرّ صغير في الظلام.

الطبقة (`night-tap`) فوق المحتوى لا تحته: وضعها تحته لا يعمل إطلاقًا لأن حاوية
المشهد تملأ الشاشة حتى حيث تبدو فارغة، فتبتلع اللمسة قبل أن تصل. ويُرفع فوقها
وحدَه ما يجب أن يبقى قابلًا للضغط: شريط المضيف، وشاشة فعله الشخصي، ولوحة الخطأ.

| المرحلة | ما تفعله اللمسة |
|---|---|
| `night-intro` | تفتح الموعد الأول |
| `night-phase-1..5` | تنقل إلى الموعد التالي |
| `night-phase-6` | تفتح خطوة المتعاونين |
| `night-accomplices` | تُغلق الليل وتفتح المرحلة السرية |

والراوي المُطفأ لا يمرّ من طابور النطق أصلًا: كان التسلسل يمشي كاملًا بلا صوت
فيعرض جملةً لم تُقَل وينتظر مُهَلها.

### قائمة الجولة عند المضيف

زرّ في شريط المضيف يفتح لوحة تعمل في أي مرحلة بعد الردهة:

- **ضبط ما يجوز ضبطه**: التعليق الصوتي وصوت الراوي ومصدر النطق ومُهَل العدّ
  والنقاش. ولا تظهر فيها طريقة النرد ولا مصطلح المواعيد ولا من يلعب — تغييرها
  وسط جولة يُنتج طاولة نصفها على قاعدة ونصفها على أخرى.
- **أنهِ الجولة**: يعود الجميع إلى الردهة وتُوزَّع الأدوار من جديد.
- **أغلق الجلسة واخرج**: يُكتب `meta.status = 'closed'`، فتقول أجهزة اللاعبين
  «انتهت الجلسة» بدل أن تنتظر مرحلةً لن تأتي.

---

## 3. تدفق الجولة خطوة بخطوة

### 3.1 إنشاء الجلسة والانضمام

```
المضيف                      Firebase                    اللاعب
  │                            │                           │
  ├─ signInAnonymously ───────►│                           │
  ├─ createRoom(code) ────────►│  rooms/{code}             │
  │◄── onValue(room) ──────────┤                           │
  │  يعرض QR + رمز + رابط      │                           │
  │                            │◄─ signInAnonymously ──────┤
  │                            │◄─ joinRoom(code,name) ────┤
  │                            │   players/{uid}           │
  │◄── players updated ────────┤                           │
  │  يعرض الاسم + الشخصية      │                    يعرض «انتظر البدء»
```

التحقق عند الانضمام (يُنفَّذ في العميل ويُدعَم بقواعد الأمان):

- الرمز موجود والغرفة في `lobby`.
- عدد اللاعبين < 8.
- الاسم غير مكرر (مقارنة بعد تطبيع المسافات).
- نفس `uid` = نفس اللاعب (إعادة اتصال، ليس لاعبًا جديدًا).
- بعد مغادرة `lobby` يُرفض أي انضمام جديد إلا عبر «استعادة لاعب» من المضيف.

### 3.2 ترتيب الجلوس

`seat` رقم من `0` إلى `n-1`، **باتجاه عقارب الساعة** حول الطاولة كما تُرى من الأعلى.

من هذا التعريف، لِلاعب في المقعد `i` ضمن `n` لاعبين:

```
neighbourLeft  = seat (i + 1) % n        ← التالي مع عقارب الساعة
neighbourRight = seat (i - 1 + n) % n    ← السابق عكس عقارب الساعة
```

هذا التعريف موجود في مكان واحد فقط: `src/game/seating.ts`.
المضيف يعيد الترتيب بالسحب والإفلات حتى يطابق الجلوس الحقيقي، وهو الوحيد الذي يكتب `seat`.

### 3.3 توزيع الأدوار

1. المضيف يضغط «ابدأ توزيع الأدوار».
2. جهاز المضيف يولّد التوزيع مرة واحدة ويكتبه في `round/secrets/{uid}` — مسار لكل لاعب
   لا يقرأه غيره.
3. كل لاعب يقرأ **مساره وحده** ويرى دوره.
4. شاشة الدور: رسم الشخصية + اسم الدور + وصف قصير + زر «فهمت دوري»، ثم إخفاء تلقائي.
5. عند وصول كل `roleAck` ينتقل المضيف تلقائيًا إلى النرد.

> **قرار معماري:** التوزيع يتم اليوم على جهاز المضيف. الواجهة معزولة خلف
> `RoleDealer` في `src/game/deal.ts` لتنتقل لاحقًا إلى Cloud Function دون تغيير الشاشات.
> راجع «المخاطر المفتوحة» في نهاية الوثيقة.

### 3.4 النرد

**رقمي:** يضغط اللاعب «ارمِ النرد» → يُقرأ الرقم المكتوب مسبقًا في مساره السري →
حركة نرد → يظهر الموعد → «عرفت موعدي» → تتحول الشاشة إلى الحالة الليلية.

**حقيقي:** يرمي اللاعب نرده في الكوب سرًا → يُدخل الرقم → يؤكد → يُقفل.
في وضع الأربعة يُدخل رقمين، وعضو الفريق يختار أحدهما.

بعد اكتمال كل الأرقام يحسب النظام `soloSlots` لكل لاعب ويكتبها في مساره السري.

### 3.5 الليل

```
المضيف                                    أجهزة اللاعبين
  │                                             │
  ├─ phase = night-intro                        │─ شاشة سوداء زرقاء، إطفاء كل شيء
  ├─ 🔊 «بدأ الليل. الجميع يغلق عينيه الآن.»     │
  ├─ phase = night-phase-1                      │
  ├─ 🔊 «أصحاب الليلة الأولى، افتحوا أعينكم»     │─ من له الموعد: اهتزاز خفيف
  ├─ 🔊 تعليمات المستيقظين                       │   الباقي: لا شيء إطلاقًا
  ├─ ⏱ عد تنازلي 10 ثوانٍ                        │
  ├─ 🔊 «أغلقوا أعينكم الآن.»                    │─ المنفرد: اهتزاز مختلف (لديك معلومة)
  ├─ phase = night-phase-2 … 6                  │
  ├─ phase = night-accomplices                  │
  ├─ 🔊 «مُخفي الملف، افتح عينيك وحدك.»          │─ جهاز المُخفي وحده: قائمة الاختيار
  ├─ 🔊 «اختر متعاونًا يقف معك.»                  │   بقية الأجهزة: لوحة الليل كما هي
  ├─ ⏱ عد ينتهي مبكرًا إن وصل الاختيار            │
  ├─ 🔊 «أغلق عينيك الآن.»                       │
  ├─ 🔊 «انتهى الليل… افتحوا أعينكم.»            │
  ├─ 🔊 (صمت) «الملف اختفى!»                     │
  └─ phase = secret-actions                     │─ تبدأ الشاشات السرية
```

خلال كل مراحل الليل، جهاز اللاعب:

- شاشة زرقاء داكنة جدًا بأقل إضاءة ممكنة.
- بلا أي نص سري يمكن قراءته من بعيد.
- تتجاهل اللمسات (لا يوجد زر فعّال).
- لا فلاش ولا إضاءة ساطعة ولا صوت.
- لا يحتاج اللاعب إلى حمل الجهاز في نافذة الاستيقاظ.

### 3.6 المرحلة السرية

بعد الليل مباشرة، **كل** جهاز يعرض شاشة شخصية بنفس الشكل والمدة تقريبًا،
حتى لا يكشف طول الانتظار من يملك معلومة:

| حالة اللاعب | الشاشة |
|---|---|
| عضو/متعاون استيقظ وحده | فحصه وقع أثناء الليل. هنا يرى نتيجته أو «لم تستعمل فحصك» |
| صار متعاونًا | إشعار سري بدوره الجديد وبمن يعرفه حسب العدد |
| المُخفي | أسماء من اختارهم |
| لا معلومة لديه | شاشة بنفس الشكل: «لا توجد معلومة لك هذه الجولة» + زر تأكيد |

**لا اختيار هنا.** الاختيار وقع في `night-accomplices` والأعين مغلقة؛ ما يبقى
لهذه المرحلة هو إيصال المعلومة بعد أن تُفتح. لذلك تتساوى الشاشات في الشكل
والمدة، ولا يكشف طول انتظار أحدٍ أنه يقرّر شيئًا.

لا ينتقل المضيف إلى النقاش حتى تصل كل `secretAck`.

### 3.7 النقاش والتصويت والكشف

```
discussion : المضيف يعرض الأسماء وترتيب الطاولة + مؤقت اختياري. الأجهزة الشخصية تنام.
voting     : كل الأجهزة تعرض قائمة اللاعبين (بدون النفس) → اختيار → قفل الشاشة.
             المضيف يعرض «صوّت 4 من 6» بلا أي تلميح لمن صوّت لمن.
reveal     : بعد آخر صوت: 3 – 2 – 1 → المكشوفون + أدوارهم + الفريق الفائز.
results    : كل الأدوار، المواعيد، من استيقظ مع من، ما فحصه كل لاعب، جدول الأصوات.
```

---

## 4. البيانات: عام مقابل سري

### 4.1 مخطط قاعدة البيانات

```
rooms/{roomCode}/
  meta/
    code, hostUid, createdAt, updatedAt, status
    phase                 ← يكتبه المضيف فقط
    resumePhase           ← المرحلة التي يُستأنف إليها بعد paused
    roundId               ← معرّف الجولة الحالية
    secretStage           ← choosing | resolved (داخل secret-actions)
  settings/
    gameName, diceMode, slotNaming, discussionSeconds,
    nightCountdownSeconds, voiceEnabled, ttsRate
  players/{uid}/
    id, name, avatarId, seat, ready, connected, lastSeen, joinedAt, isHost
  round/                  ← الجولة الحالية فقط؛ `resetRound` يمسحها ويجدّد roundId
    progress/{uid}/       ← أعلام فقط، بلا محتوى سري
      roleAck, diceAck, secretAck, voted
    secrets/{uid}/        ← 🔒 يقرؤه صاحبه (والمضيف — انظر أدناه)
      playerId, role, dice[], chosenSlot, effectiveSlots[], soloSlots[],
      inspection{ targetId, side, revealedSlot }, knownAllies[],
      accompliceQuota, accompliceCandidates[], accompliceChoice[], becameAccomplice
    votes/{uid}           ← 🔒 كتابة من صاحبه مرة واحدة، قراءة للمضيف فقط قبل الكشف
    results/              ← يُكتب مرة واحدة عند الكشف، ثم يصبح عامًا
      tally, winner, hiderId, accompliceIds[], reveal[]
```

### 4.2 ما يستطيع كل طرف قراءته

| المسار | المضيف | صاحب المسار | بقية اللاعبين |
|---|---|---|---|
| `meta` | قراءة/كتابة | قراءة | قراءة |
| `settings` | قراءة/كتابة | قراءة | قراءة |
| `players/{uid}` | قراءة + كتابة `seat` | قراءة + كتابة بياناته | قراءة (اسم/شخصية/اتصال) |
| `round/progress/{uid}` | قراءة | قراءة/كتابة | قراءة (أعلام فقط) |
| `round/secrets/{uid}` | قراءة/كتابة ⚠️ | قراءة + كتابة محدودة | ❌ **ممنوع تمامًا** |
| `round/votes/{uid}` | قراءة | كتابة مرة واحدة | ❌ |
| `round/results` | كتابة | قراءة بعد الكشف | قراءة بعد الكشف |

> ⚠️ **المضيف يقرأ الأسرار في النسخة الحالية، وهذا مقصود ومحدود.**
> ثلاث عمليات لا يمكن تنفيذها بدونه ما دام لا يوجد Cloud Function:
> حساب الاستيقاظ المنفرد، تحويل المختارين إلى متعاونين، وكشف موعد الجار عند الفحص
> — كلها تعدّل أو تقرأ أسرار **أكثر من لاعب**، وجهاز اللاعب لا يملك هذا الحق إطلاقًا.
> العزل بين اللاعبين أنفسهم كامل ومطبَّق في القواعد. إزالة ثقة المضيف تتطلب نقل
> `src/game/director.ts` إلى Cloud Functions (خطة Blaze) — وهي مصمَّمة لذلك أصلًا.

**الكتابة المحدودة لصاحب السر:** يكتب اللاعب في مساره اختيار موعده (وضع الأربعة)،
نتيجة نرده الحقيقي قبل `diceAck`، طلب فحص جاره، واختياره للمتعاونين إن كان المُخفي.
القواعد تمنعه من تعديل `role` أو `soloSlots` أو `knownAllies` أو تعديل النرد بعد التأكيد.

القواعد الفعلية في [`firebase/database.rules.json`](firebase/database.rules.json).

### 4.3 ما يُمنع على العميل

- كتابة أو تعديل دوره.
- تعديل نتيجة نرده بعد `diceAck`.
- كتابة أو قراءة صوت لاعب آخر.
- قراءة `secrets` لاعب آخر.
- تعديل `phase` (إلا المضيف).
- التصويت لنفسه.
- الانضمام بعد مغادرة `lobby`.

---

## 5. الاتصال والانقطاع

### الحضور

كل جهاز يكتب `players/{uid}/connected = true` ويسجّل `onDisconnect` يعيدها `false`
مع ختم `lastSeen`. المضيف يعرض ثلاث حالات: متصل · ضعيف · منقطع.

### إعادة الانضمام

هوية اللاعب = `uid` المصادقة المجهولة، وهي ثابتة في نفس المتصفح.
يُحفظ `{roomCode, uid}` في `localStorage` ليُعاد فتح الجلسة تلقائيًا.
عند العودة يستأنف الجهاز من مرحلة الغرفة الحالية، **ولا يُعاد عرض دوره تلقائيًا** —
يظهر زر «أظهر دوري» يتطلب لمسة واعية.

### المصفوفة

| الحدث | الجهاز المتأثر | السلوك |
|---|---|---|
| قفل الشاشة | لاعب | يعود لنفس المرحلة، بلا كشف تلقائي للدور |
| إعادة تحميل الصفحة | لاعب | استعادة صامتة من `localStorage` |
| ضعف الإنترنت | أي | شريط «إعادة اتصال…» + تعطيل الأزرار |
| خروج لاعب | لاعب | `connected=false`؛ المضيف يرى بطاقته باهتة |
| انقطاع أثناء الليل | لاعب | **المضيف يوقف الجولة** أو ينتظر — لا يُكمَل الليل |
| انقطاع المضيف | الغرفة | تتجمد على آخر مرحلة؛ يستعيدها المضيف بنفس الرمز |
| جهاز ثانٍ بنفس الهوية | لاعب | الأقدم يُفصل برسالة واضحة |
| محاولة العودة لمرحلة سابقة | لاعب | مرفوضة — الشاشة تتبع `meta/phase` |

---

## 6. الصوت

`AudioManager` مركزي يعمل على جهاز المضيف فقط:

- مصدر النصوص: `src/audio/script.ar.ts` — كل جملة لها `id` ونص ومسار ملف صوتي اختياري.
- الترتيب: ملف مسجّل إن وُجد ← وإلا Web Speech API ← وإلا نص فقط على الشاشة.
- **لا يفشل التطبيق** إذا غاب TTS: العد التنازلي والانتقالات تعمل كما هي.
- تسميات تعتمد `settings.slotNaming` فتتغير الجملة كلها بتبديل واحد.
- كل جملة معروضة كـ Caption متزامن أسفل شاشة المضيف.
- تحكم: تشغيل/إيقاف، إعادة الجملة، مستوى الصوت، سرعة العد.

قائمة الجمل المطلوبة: الترحيب، الاستعداد، إغلاق العيون، الليالي 1–6، تعليمات المستيقظين،
العد التنازلي، إغلاق العيون، المتعاونون، نهاية الليل، اختفاء الملف، النقاش، التصويت، النتيجة.

---

## 7. طبقة النقل

الشاشات لا تعرف Firebase. كلها تتعامل مع واجهة واحدة في `src/net/transport.ts`:

```ts
interface RoomTransport {
  createRoom(...), joinRoom(...), watchRoom(cb), watchSecrets(uid, cb),
  setPhase(phase), updatePlayer(uid, patch), writeSecrets(map),
  ack(uid, key), submitVote(uid, target), writeResults(results), leave()
}
```

| التنفيذ | الاستخدام |
|---|---|
| `FirebaseTransport` | الإنتاج — RTDB + مصادقة مجهولة + قواعد أمان |
| `LocalTransport` | التطوير والاختبار — `BroadcastChannel` + `localStorage`، عدة تبويبات على جهاز واحد بلا أي إعداد |

يُختار التنفيذ تلقائيًا: إن وُجدت متغيرات `VITE_FIREBASE_*` تُستخدم Firebase، وإلا النقل المحلي
مع شارة واضحة على الشاشة تبيّن الوضع الحالي.

---

## 8. المخاطر والقرارات المفتوحة

1. **المضيف طرف موثوق مؤقتًا.** جهاز المضيف يولّد الأدوار ويحسب الانفراد ويحوّل
   المتعاونين ويكشف مواعيد الجيران، فهو يقرأ كل الأسرار. قواعد الأمان تمنع أي **لاعب**
   من قراءة سر غيره منعًا تامًا، لكنها لا تحمي من مضيف يعبث بالكود.
   الحل النهائي: نقل `src/game/director.ts` إلى Cloud Functions (يحتاج خطة Blaze).
   المنطق كله دوال نقية مفصولة عن الشبكة تحديدًا ليتم هذا النقل بلا إعادة كتابة.
2. **شاشة الانتظار في المرحلة السرية.** جُعلت كل الشاشات بنفس الشكل والزر لتقليل التلميح،
   لكن اللاعب سريع التأكيد ما زال يمكن ملاحظته. البديل: مدة صامتة موحّدة للجميع.
3. **دقة `onDisconnect`** على شبكات الجوال قد تتأخر ثوانٍ؛ حالة «ضعيف» تغطي الفجوة.
4. **TTS العربي** يختلف بين iOS وAndroid ومتصفحات سطح المكتب. الحل النهائي تسجيلات بشرية،
   والمسارات جاهزة لها في ملف النصوص.
5. **الهوية البصرية.** لا يوجد شعار أو ألوان رسمية لقوسي داخل المشروع، ولم يُخترع شعار.
   كل الألوان في Design Tokens مركزية للاستبدال لاحقًا.
